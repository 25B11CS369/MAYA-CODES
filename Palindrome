#include <stdio.h>
int isPalindrome(int num) 
{
    int original = num, rev = 0, r;
    while (num != 0) 
    {
        r= num % 10;
        rev = rev * 10 + r;
        num /= 10;
    }
    return original == rev;
}
int main() 
{
    int n;
    scanf("%d", &n);
    if (isPalindrome(n))
        printf("Palindrome");
    else
        printf("Not Palindrome");
    return 0;
}
