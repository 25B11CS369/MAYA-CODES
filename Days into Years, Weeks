#include<stdio.h>
int main() 
{
    int days, years, weeks, remaining_days;
    scanf("%d", &days);
    years = days / 365;
    remaining_days = days % 365;
    weeks = remaining_days / 7;
    printf("%d", years);
    printf("%d", weeks);
    return 0;
}
